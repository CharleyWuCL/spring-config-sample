spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8888
      profile: test
    stream:
      kafka:
        binder:
          zk-nodes: localhost:2181
          brokers: localhost:9092
